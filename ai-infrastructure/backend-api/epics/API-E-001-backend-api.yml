schema_version: 1
id: API-E-001
title: "Backend API"
dept: backend-api
phase: 2
description: "FastAPI endpoints with house rules, auth/sessions and invite tokens, and admin seeding, built against the v1 schema. Auth/sessions + admin bootstrap (API-T-002) is done; the v1 issue/view/label/epic endpoint surface (API-T-001) and invite-token user creation (API-T-003) remain, each running as a test-design dispatch followed by an implementation dispatch (ADR-016)."
adrs: [10, 11, 13]
