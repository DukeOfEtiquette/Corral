---
schema_version: 1
department: "database"
---

# Status

Current phase, next step, and blocked work are derived from the roadmap (`epics/`/`phases/` files) and the `tasks/blocked/` trees and are shown on the project-manager dashboard and its `data.json` (ADR-040); they are no longer hand-authored here. Activity history is git-derived (ADR-039). This file now carries only its frontmatter and this pointer.
