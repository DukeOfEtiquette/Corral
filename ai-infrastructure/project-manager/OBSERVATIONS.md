# Observations

Append-only log of recurring patterns, friction points, and notable one-offs seen while working in this repo. Convention inherited from rogue (ADR-009).

## Conventions

- Stable IDs: `COR-NN`, monotonically increasing, never reused.
- Lifecycle: **seen-once** (handled ad hoc, not yet logged) -> **logged** (an entry below, with context) -> **promoted** (canonicalized into a rule, template, role doc, or ADR; the entry records where it went).
- Entries are never edited after the fact except to update their lifecycle state and promotion pointer.

## Entry format

```markdown
### COR-NN: short title
- date: YYYY-MM-DD
- state: logged | promoted -> <where>
- context: what happened, where
- pattern: why this might recur / what to do about it
```

## Log

### COR-01: pending-ADR leaning text drifts against sibling docs

- date: 2026-06-05
- state: promoted -> `docs/ai-orchestration/roles/ORCHESTRATOR-ROLE.md`, "Stale-reference sweep when resolving ADRs" bullet under "Kickoff drafting convention" (promoted 2026-06-05 after occurrences in COR-T-002 (tasks/README vs ADR-018 leaning), COR-T-003 pre-kickoff (README roadmap vs ADR-010 leaning), and COR-T-003 review (ADR-004 consequence staleness))
- context: While resolving anticipated decisions for COR-T-002 (ADR-012 schema), the Orchestrator found `./tasks/README.md` (migration mapping) pinning `priority` as an issue column while `./decisions/ADR-018-department-label-taxonomy.md` (Option A leaning text) listed `priority:P0..P3` as a reserved label family. Both were written during Phase 0 bootstrap; neither was a taken decision. Resolved with the user 2026-06-05: priority is a first-class column, and the COR-T-002 kickoff directs ADR-012's Consequences to record the narrowing of ADR-018's open question.
- pattern: Pending ADRs carry leaning text and examples drafted before any decision was taken; sibling pending ADRs and convention docs can encode contradictory assumptions about the same dimension. Each resolved ADR can silently invalidate a neighbour's framing. When resolving any pending ADR, sweep its `related_adrs` and the convention docs it touches for contradicted leanings, surface conflicts to the user as part of decision resolution, and record narrowings in the accepted ADR's Consequences section.

### COR-02: orchestrator-direct ADR resolution follows a repeatable seven-step flow

- date: 2026-06-10
- state: promoted -> `docs/ai-orchestration/roles/ORCHESTRATOR-ROLE.md`, "Pending-ADR resolution playbook" subsection (promoted 2026-06-10 via COR-T-019, after two clean instances: COR-T-008 (ADR-018) and COR-T-009 (ADR-025))
- context: COR-T-008 and COR-T-009 each took a pending ADR to accepted as orchestrator-direct work (the `decisions/` carve-out). Both ran the identical shape: read the pending ADR and its `related_adrs`; do the homework and form grounded recommendations; frame only the binding decisions with the user while letting the mechanical ones flow; set the ADR pending -> accepted; forward-pointer sweep both directions (subsuming COR-01); STATUS hygiene plus the Next-step/roadmap delta; and a two-commit close (accept, then move-to-done citing the accept hash). The shape was stable enough across the two runs to canonicalize.
- pattern: Resolving a pending ADR is a recurring orchestrator-direct task type with a fixed sequence, not an ad hoc activity. Promoted into the role doc so future orchestrators follow the seven steps rather than re-deriving them; the playbook cross-references COR-01's stale-reference sweep bullet (step 5) rather than duplicating it. The two-commit close (step 7) is the reusable mechanism for recording a deliverable's hash in its own done line.

### COR-03: hand-maintained roadmap milestone statuses are the dashboard's last drift surface, now load-bearing

- date: 2026-06-11
- state: promoted -> `./decisions/ADR-036-work-item-taxonomy.md` (2026-06-12; the taxonomy makes epic/phase status a task-rollup derivation and retires hand-set milestone status. COR-T-040 already derives a milestone's effective status from its task refs; the roadmap restructure follow-on completes the removal of the hand-set field.) | terminal: the last hand-maintained STATUS activity surface (last_updated + recent_updates frontmatter keys) is now git-derived per ADR-039; retired by COR-T-047 (2026-06-13).
- context: The dashboard originally derived each phase's CURRENT/UPCOMING badge from a single hand-maintained `phase` field in coordinator STATUS frontmatter; that field drifted (stayed `phase: 1` after Phase 2 began via COR-T-023 and DB-T-001), so the live dashboard showed Phase 1 CURRENT while Phase 2 was already underway. The same class of drift had been seen once before (P1-6 milestone status left `in-progress` after COR-T-017 delivered it, corrected during the COR-T-020 wrap-up; recorded in STATUS recent_updates). COR-T-029 fixed the phase pointer by deriving `current_phase`, `current_phase_title`, and `next_step` from the per-milestone `status` values. But that pushed the hand-maintained surface down one level: every derived field now depends on the roadmap milestone `status` (`done`/`in-progress`/`planned`) values, which are still set by hand in the coordinator STATUS frontmatter and are read verbatim by `etl.py` (`ms.get("status")`). A milestone whose work is done but whose status was never flipped will now silently mis-drive the entire phase display, not just one badge.
- pattern: Each time a hand-maintained pointer is replaced by derivation, the drift relocates to whatever the derivation now reads, rather than disappearing. The remaining hand-set inputs are the milestone statuses. Many milestones carry a `task:` ref (e.g. P1-4 -> COR-T-014, P2-1 -> DB-T-001), so a milestone's `done`-ness could be cross-checked against (or derived from) whether that task file sits in a `tasks/done/` tree, catching the drift mechanically. Candidate promotion: a consistency check in `etl.py` (or a dashboard warning) that flags any `task`-bearing milestone whose authored status disagrees with its task's tree location; full auto-derivation is bounded because task-less milestones have no structural signal. Until promoted, milestone statuses remain a manual-accuracy dependency worth re-checking when advancing the roadmap. Separately, the dead department PHASE column (a hand-maintained field with no source at all) is tracked as the deliverable COR-T-030, distinct from this milestone-status surface.

### COR-04: workers emit plausible-but-wrong run/verification commands for unrun steps

- date: 2026-06-11
- state: promoted -> `./decisions/ADR-035-cited-reference-integrity-dispatched-work.md` (2026-06-12; prevention via the kickoff citation-completeness convention plus the explicit Dispatched-worker-flow step-6 sub-step; implementation cascade COR-T-039)
- context: On COR-T-034 (dashboard ADR-modal), the dispatched `worker-agent` could not run the ETL or Vite build (ADR-003 compose-only run policy; the worker correctly declined to fabricate a verification result). But in its closing report's "Build / verification status" it handed the Orchestrator two specific commands to run instead, `docker compose ... run --rm etl` and `... run --rm build`, naming `etl` and `build` compose services. The dashboard `ai-infrastructure/project-manager/dashboard/docker-compose.yml` defines only a single `dashboard` service (build + entrypoint ETL + http.server); no `etl` or `build` service exists, so both suggested commands would fail. The Orchestrator caught it on the verify-against-disk re-derivation (Dispatched-worker flow step 6) and substituted the correct single command, `docker compose -f .../docker-compose.yml up --build`.
- pattern: A worker that correctly refuses to fabricate a verification *result* may still fabricate the verification *procedure*, inventing command/service/flag names that read as authoritative but were never checked against the actual compose/Makefile/script. The unverified-claim risk shifts from "did the check pass" to "is this even the right command", and a report's run instructions are exactly the kind of load-bearing claim the Orchestrator must re-derive rather than trust (Agent Discipline; Dispatched-worker flow step 6 already says do not trust the report's verification claims, of which run procedures are one). This is the first logged instance; a second would warrant promotion. Candidate promotions: (a) a kickoff convention that the Orchestrator pins the exact run/verify command(s) in the kickoff when the worker cannot run them, so the worker echoes a checked command rather than inventing one; or (b) a worker-close-checker rule that any report-supplied run command naming a compose service / script target be grounded in a file the worker actually read. Until promoted, the Orchestrator verifies report-supplied run commands against the compose/script files on disk before relaying them to the user.

### COR-05: workers emit self-stale follow-ups describing pre-edit state they already changed

- date: 2026-06-12
- state: promoted -> `./decisions/ADR-035-cited-reference-integrity-dispatched-work.md` (2026-06-12; promoted as part of the COR-04/05/06 family. Note: self-stale follow-ups are not a citation/command issue, so ADR-035's prevention does not cover this entry directly; it stays covered by the unchanged step-6 re-derivation backstop, which ADR-035 makes explicit for deliverable citations)
- context: On COR-T-035 (authoring the test-designer agent), the dispatched worker-agent edited `.claude/agents/worker-close-checker.md`'s Pipeline Position diagram, changing the top node from "Worker (Sonnet)" to "Worker / Test Designer" and correcting the dispatch lines to the orchestrator-run model. But its closing report's Follow-ups flagged that the diagram "still labels the top node Worker (Sonnet)" and recommended widening it, describing the pre-edit state the same session had already fixed. Caught by the Orchestrator's verify-against-disk pass (Dispatched-worker flow step 6): the on-disk diagram already read "Worker / Test Designer", so the follow-up was moot.
- pattern: A worker's Follow-ups (and other report claims) can lag its own edits, narrating an earlier point in the session rather than the final disk state. Cousin of COR-04 (workers emit plausible-but-wrong report claims that fail verify-against-disk); the shared umbrella is that a worker report's state-claims are unverified narration and the Orchestrator's independent re-derivation (Dispatched-worker flow step 6) is the backstop, which is already canonized. First logged instance of this specific sub-pattern (self-stale follow-up). Candidate promotion if it recurs: a worker self-check that each Follow-ups item be re-validated against final disk state before the report is written, or a close-checker cross-check. Tracked as a second data point alongside COR-04 per the user (2026-06-12); not promoted.

### COR-06: workers fabricate file paths cited into the deliverable for files they were not given to read

- date: 2026-06-12
- state: promoted -> `./decisions/ADR-035-cited-reference-integrity-dispatched-work.md` (2026-06-12; the worked example for ADR-035's prevention rule (pin the cited path in the kickoff) and the explicit step-6 deliverable-path-resolution sub-step; implementation cascade COR-T-039)
- context: On COR-T-038 (authoring `./END-GOAL.md`), the dispatched executor wrote two ADR cross-reference links into the deliverable by full path: `./ai-infrastructure/project-manager/decisions/ADR-008-dogfood-milestone-self-hosting.md` and `.../ADR-021-candidate-department-menu.md`. Neither file was in the executor's `explicit_reads` (only ADR-033 and ADR-034 were); it reconstructed the filenames from the ADR numbers and guessed the title slug. Both were wrong (actual: `ADR-008-bootstrap-tasks-dogfood-milestone.md`, `ADR-021-candidate-departments.md`), so both rendered as broken links. The one cited path the executor had actually read (ADR-032) was correct. Caught by the Orchestrator's verify-against-disk pass (Dispatched-worker flow step 6), which resolved every `./`-path in END-GOAL.md against disk and found the two misses; corrected inline before close, then all 7 links re-verified.
- pattern: Third data point in the COR-04 / COR-05 family (worker state-claims are unverified narration; Dispatched-worker flow step 6 is the backstop), but a distinct sub-pattern: the fabricated path lands in the shipped *deliverable*, not in the report. An executor asked to cite a file it was not handed will reconstruct the path from a naming convention (here `ADR-NNN-kebab-title`) and guess the slug, which often diverges from the real filename. Because the artifact is a baked-in link rather than a prose claim, the backstop must resolve every cited repo-relative path on disk, not just spot-check prose. Candidate promotions: (a) a kickoff convention that when the kickoff asks the executor to cite a file, that file's exact path is included in `references`/`explicit_reads` so the executor echoes a checked path rather than reconstructing one; or (b) a worker-close-checker rule that every repo-relative path appearing in a deliverable resolve on disk. Until promoted, the Orchestrator resolves every cited path in a deliverable against disk before close. Tracked as a third data point in the family per the user (2026-06-12); not promoted.

### COR-07: visual deliverables have a verification blind spot that only a render catches

- date: 2026-06-12
- state: logged
- context: On COR-T-040 (dashboard roadmap ref badges) the dispatched executor verified the data layer (curl on `data.json`, compose build succeeds) and returned COMPLETED, but the page rendered completely BLANK: a React reserved-prop bug (`ref` passed as a data prop is not forwarded to a function component, so the badge data was `undefined` and the render threw, unmounting the whole tree). No data-layer or build check could surface it. Separately on the same task, styling defects (the Agent Fleet tables not aligning across the two groups; milestone rows lacking divider lines) were invisible to non-visual verification. All were caught only when the Orchestrator headless-rendered the running dashboard (`google-chrome --headless=new --screenshot`, then Read and cropped the PNG) during verify-against-disk (Dispatched-worker flow step 6). The COR-T-041 reshape reinforced the pattern: the Orchestrator headless-rendered each pass before the user gate, catching the E2.1 rollup color and confirming the dept badges.
- pattern: A visual deliverable's correctness lives in the rendered DOM, which the executor's data-layer + build verification structurally cannot reach: render-time crashes (reserved props, undefined access) and layout/styling correctness are invisible to it. The executor's verification was sound for what it covered (data shape, build success); the gap is intrinsic to non-visual checks of a visual surface. The user's visual gate is the backstop, but bouncing to the user for defects a render would catch is expensive and erodes trust. Mitigation established this session: the Orchestrator headless-renders the running deliverable (`google-chrome --headless=new --virtual-time-budget=<ms> --screenshot=<png> <url>`, then Read the PNG, cropping regions with ImageMagick for detail) during step 6, before surfacing to the user. Candidate promotions: (a) a written sub-step in the Dispatched-worker flow step 6 ("for a visual deliverable, headless-render and inspect the running surface before the user gate"); and/or (b) an executor convention that a visual-surface executor attempt a headless render as part of its own verification. Until promoted, the Orchestrator renders visual deliverables before the user gate. Logged after two reinforcing instances (COR-T-040 blank page + styling; COR-T-041 reshape) per the user (2026-06-12).
